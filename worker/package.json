{
  "name": "git-hotspots-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "git-hotspots as a Cloudflare Worker (GitHub REST API, no git binary)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.137.0"
  }
}
